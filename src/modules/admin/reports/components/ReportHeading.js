import { styled } from '@material-ui/core';

const ReportHeading = styled('h1')({
    fontWeight: 'bold',
    fontSize: '2.5rem',
    textAlign: 'center',
    margin: 0
});

export default ReportHeading;