import { PRIMARY_COLOR } from 'shared/style';
import styleSettings from "reactor/layout/utils/style-settings";

export const MAIN_COLOR = PRIMARY_COLOR;

styleSettings.set({
    fontFamily: {
        ar: {
        },
    },
});